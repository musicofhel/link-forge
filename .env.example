# Discord
DISCORD_TOKEN=your_bot_token_here
DISCORD_CHANNEL_ID=your_channel_id_here
DISCORD_GUILD_ID=your_guild_id_here

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=link_forge_dev

# Paths
SQLITE_PATH=./data/queue.db

# Processing
PROCESSOR_POLL_INTERVAL_MS=5000
SCRAPE_TIMEOUT_MS=15000
CLAUDE_TIMEOUT_MS=60000

# Taxonomy
TAXONOMY_CHECK_INTERVAL_MS=3600000
TAXONOMY_SPLIT_THRESHOLD=20

# Local inbox (drop files into this folder for automatic ingestion)
INBOX_ENABLED=true
INBOX_DIR=./data/inbox
INBOX_POLL_INTERVAL_MS=5000
INBOX_AUTHOR_NAME=aaron

# Google Drive (optional — document ingestion from per-user subfolders)
GDRIVE_ENABLED=false
GDRIVE_SERVICE_ACCOUNT_KEY_PATH=
GDRIVE_SHARED_FOLDER_ID=
GDRIVE_POLL_INTERVAL_MS=60000
GDRIVE_UPLOAD_DIR=./data/uploads

# Dashboard security (all three required for password auth)
DASHBOARD_GUID=               # Random UUID — becomes the dashboard URL path (/d/<guid>)
DASHBOARD_PASSWORD_HASH=      # bcrypt hash of the password (generate: node -e "console.log(require('bcryptjs').hashSync('yourpass', 12))")
DASHBOARD_SESSION_SECRET=     # Random string for signing session cookies
DASHBOARD_CORS_ORIGIN=        # Allowed CORS origin (blank = same-origin only)

# Logging
LOG_LEVEL=info
